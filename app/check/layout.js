export const metadata = {
  title: "Checkout Page",
  description: "Checkout Page - All For Us",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
